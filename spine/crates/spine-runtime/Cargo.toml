[package]
name = "spine-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
spine-core = { path = "../spine-core" }
spine-worker = { path = "../spine-worker" }
tokio = { version = "1", features = ["full"] }
tracing = "0.1"

[dev-dependencies]
tokio-test = "0.4"
serde_json = "1.0"
